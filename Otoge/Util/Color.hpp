#pragma once

// 色のプリセット
namespace color_preset
{
    /* グレースケール */
    const unsigned BLACK = GetColor(33, 33, 33);
    const unsigned WHITE = GetColor(250, 250, 250);
    const unsigned GREY = GetColor(158, 158, 158);
    const unsigned WHITE_GREY = GetColor(245, 245, 245);
    const unsigned DARK_GREY = GetColor(66, 66, 66);
    const unsigned LIGHT_GREY = GetColor(224, 224, 224);

    /* 緑系 */
    const unsigned GREEN = GetColor(76, 175, 80);
    const unsigned WHITE_GREEN = GetColor(129, 199, 132);
    const unsigned LIGHT_GREEN = GetColor(139, 195, 74);
    const unsigned LEAF_GREEN = GetColor(165, 214, 167);

    const unsigned TEAL = GetColor(0, 150, 136);

    /* 青系 */
    const unsigned BLUE = GetColor(33, 150, 243);
    const unsigned BLUE_GREY = GetColor(96, 125, 139);
    const unsigned DARK_BLUE_GREY = GetColor(38, 50, 56);
    const unsigned WHITE_BLUE = GetColor(100, 181, 246);
    const unsigned DARK_BLUE = GetColor(13, 71, 161);
    const unsigned LIGHT_BLUE = GetColor(3, 169, 244);
    const unsigned INDIGO = GetColor(63, 81, 181);
    const unsigned WHITE_INDIGO = GetColor(92, 107, 192);
    const unsigned CYAN = GetColor(0, 188, 212);

    const unsigned PURPLE = GetColor(156, 39, 176);

    /* 赤系 */
    const unsigned RED = GetColor(244, 67, 54);
    const unsigned PINK = GetColor(233, 30, 99);
    const unsigned ORANGE = GetColor(255, 152, 0);
    const unsigned DEEP_ORANGE = GetColor(255, 87, 34);
    const unsigned AMBER_ORANGE = GetColor(255, 193, 7);
    const unsigned YELLOW = GetColor(255, 235, 59);
    const unsigned LEMON_YELLOW = GetColor(255, 245, 157);
}